---
'@bgd-labs/icons': patch
'@bgd-labs/icons-react': patch
---

Fix light halo around the MEGA token icon: rebuild the full variant so the perimeter is a single edge (black disc under an inner gray disc) instead of a background disc with a nearly-coincident ring clipped on top.
